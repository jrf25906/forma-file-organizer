import TornadoPrototype from '@/components/effects/TornadoPrototype';

export const metadata = {
  title: 'Tornado Prototype | Forma',
};

export default function TornadoPage() {
  return (
    <main className="w-full min-h-screen">
      <TornadoPrototype />
    </main>
  );
}
